# Telegram bot token (get it from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Trading analysis API — base URL and endpoint
ANALYSE_BASE_URL=http://127.0.0.1:8001
ANALYSE_ENDPOINT=/api/v1/analyse

# Request timeout in seconds (the analysis stack is heavy; allow enough time)
ANALYSE_TIMEOUT=120

# Shared password gate — anyone messaging the bot must send this password to
# unlock it. Leave empty to keep the bot open to everyone.
BOT_PASSWORD=

# How long an unlock lasts, in hours (default: 24 = one day)
SESSION_TTL_HOURS=24

# Chats that skip the password gate entirely — comma-separated numeric Telegram
# chat IDs (send /chatid inside a chat to get its ID). Example:
#   FREE_CHAT_IDS=-1001234567890,-1009876543210
# Leave empty to require the password in every chat.
FREE_CHAT_IDS=